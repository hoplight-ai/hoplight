# Copy to .env.local for local runs. Production values live in the Vercel project, never in git.

# Where /api/intake forwards contact-form submissions. A Make.com custom webhook URL.
# Set with: bash scripts/set-vercel-env.sh MAKE_INTAKE_WEBHOOK
MAKE_INTAKE_WEBHOOK=
